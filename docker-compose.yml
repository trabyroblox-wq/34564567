version: '3.8'

services:
  proxy:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - backend/.env
    volumes:
      - ./backend:/app
    restart: unless-stopped
